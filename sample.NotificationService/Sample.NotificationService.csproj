<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<TargetFrameworks>net10.0-ios</TargetFrameworks>

		<OutputType>Library</OutputType>
		<RootNamespace>Sample.NotificationService</RootNamespace>
		<IsAppExtension>true</IsAppExtension>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>

		<ApplicationId>com.emarsys.maui.sample.NotificationService</ApplicationId>

		<ApplicationDisplayVersion>1.0</ApplicationDisplayVersion>
		<ApplicationVersion>1</ApplicationVersion>

		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'ios'">15.0</SupportedOSPlatformVersion>

		<CodesignKey>Apple Development: (4J5FXBB97U)</CodesignKey>
		<CodesignProvision>iOS Team Provisioning Profile: *</CodesignProvision>
		
		<!-- Workaround for AOT compilation issue https://github.com/dotnet/runtime/issues/109966 -->
		<MtouchInterpreter>all</MtouchInterpreter>
		<Registrar>managed-static</Registrar>
	</PropertyGroup>	
	
	<ItemGroup>
		<None Include="Info.plist" />
	</ItemGroup>

	<!-- Reference to Emarsys Binding project -->
	<ItemGroup>
		<ProjectReference Include="..\common\Public\Emarsys.Binding.csproj" />
	</ItemGroup>
</Project>
